import 'package:flutter/material.dart';

class PreviewSetting extends StatelessWidget {
  const PreviewSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}